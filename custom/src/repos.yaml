---
odoo:
  defaults:
    depth: $DEPTH_DEFAULT
  remotes:
    origin: $DEFAULT_REPO_PATTERN_ODOO
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

OCA/web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1822/head
OCA/contract:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/contract.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
OCA/reporting-engine:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/reporting-engine.git
    sfit: https://github.com/sunflowerit/reporting-engine.git
  target: origin $ODOO_VERSION
  merges: 
    - origin $ODOO_VERSION
    - sfit refs/pull/1/head

OCA/hr:
 defaults:
   depth: $DEPTH_MERGE
 remotes:
    origin: https://github.com/OCA/hr.git
    sfit: https://github.com/sunflowerit/hr.git
 target: origin $ODOO_VERSION
 merges:
  - origin $ODOO_VERSION
  - sfit 13.0-hr_contract_report_dynamic

